echo "##################################################"
echo "please run brew bundle to install any dependencies"
echo "##################################################"
sudo chsh -s $(which zsh) ${USER}

eval zsh